/**
 * Safe Order — /customer/login uses the same shared Login page as the merchant
 * route. The shell picks the customer tab automatically based on the URL path.
 */
export { default } from '../merchant/Login'
